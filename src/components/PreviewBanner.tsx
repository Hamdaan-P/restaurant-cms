export function PreviewBanner() {
  return (
    <div className="sticky top-0 z-50 border-b border-amber-500 bg-amber-400 px-6 py-3 text-center text-sm font-semibold text-amber-950">
      PREVIEW MODE — showing draft and published content. This is not the
      live site.
    </div>
  );
}
