RESTAURANT CMS — ZaryahPlus Task 4
Progress log

PHASE 0 ✅ Concepts, decisions, accounts (Neon, Cloudinary, Vercel)
PHASE 1 ✅ Repo, Next.js, Neon connected, 6 models migrated
PHASE 2 ✅ Public site complete (22 July 2026)

Phase 2 details:
- src/lib/prisma.ts — shared Prisma client singleton (dotenv + PrismaPg adapter, Prisma 7)
- prisma/seed.ts — destructive seed, run manually only
- 5 pages, all reading live from Neon, all force-dynamic:
    /          Home     — hero + featured dishes via featured:true reference
    /menu      Menu     — list, status + order
    /gallery   Gallery  — list, status + order
    /about     About    — About record + StaffMember list (two queries)
    /contact   Contact  — single record
- app/layout.tsx — shared nav (hardcoded by design), title from SiteSettings
- Schema gaps found and fixed: status added to StaffMember; SiteSettings model added

Proven manually:
- Edit in Prisma Studio → refresh → site updates, no code change
- order change → dish moves position
- status DRAFT → item disappears, page fine
- one price edit → updates on /menu AND / (reference, not copy)

Known / deferred:
- placehold.co images + plain <img>; switch to next/image + remotePatterns in Phase 4 (Cloudinary)
- pg SSL warning — forward-compatibility notice, no action needed

Environment notes:
- Terminal 1 = npm run dev · Terminal 2 = one-off commands
- After any prisma generate, restart the dev server

NEXT: Phase 3 — admin panel, Auth.js login, schema-driven form-maker, confirmations

