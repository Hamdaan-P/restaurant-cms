## === REVIEW SESSION R2 — 29 July 2026 ===

BLOCK 3 — DONE + COMMITTED + PUSHED (c3e3948)
Real draft/publish flow via publishedData JSON snapshot (Shape 1, not revisions table).
- publishedData Json? column added to all public content types.
- Migration backfills existing PUBLISHED rows → no live content lost.
- Publish writes a registry-driven snapshot; Save as draft leaves publishedData untouched.
- Public reads now key off "publishedData IS NOT NULL", not status = PUBLISHED.
- Vitest added; test asserts publish snapshots + draft preserves live copy.
- Break-on-purpose confirmed: draft-writes-snapshot break made the test go red; reverted → green.
- Verified live (incognito): draft edit stays hidden, published copy survives, publish pushes new snapshot.
- README draft→preview→publish claim is now true as written.

ALL THREE BLOCKERS NOW CLEARED (Block 1, 2, 3 done).

STILL OPEN — Majors (next session):
- 4  error handling in actions/handlers
- 5  numeric bounds + wrong invalid message
- 6  enforce single-instance types in schema
- 7  server-side upload restrictions (signed preset)
- Tests: coerceField, reorder, delete-compaction

Minors (8–12): order base, orphaned Cloudinary assets, caching contradiction, README relation accuracy, route matcher lookahead.

Known limitation logged: Block 3 test runs against the real dev DB (cleans up after itself). Dedicated test DB = proper fix, later.
Still pending from Block 1: strip old credentials from git history (README already scrubbed).